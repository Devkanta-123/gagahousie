name: gaga_housie
description: GaGa Housie Tambola Game
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  image_picker: ^1.0.4  # Add this line
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Add your assets here
  assets:
    - assets/qr_scanner.png
    # Add more assets as needed
    # - assets/images/
    # - assets/icons/